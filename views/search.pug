doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet' type='text/css' href='guiCrud.css')
  script(src='logik.js')
  title Search
body
    button Kasse
    button Faktura
    button Crud
    h1 PaperSign Search
    .parent

        h2 søgning
        form.div2(action = "/search", method="get")
            input#atribut(type='text' name="attribut" placeholder='attribute')
            input#value(type='text' name="vaerdi" placeholder='soegevaerdi')
            input(class='button' type="submit", value="🔍👀 Søg")
            <br>
        each data in search
            a#productHeader(class='productHeader' href='/CRUD/'+ data.docID +'')= 'Navn: ' + data.navn
            <br>
            