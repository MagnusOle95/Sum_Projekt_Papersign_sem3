doctype html
head
  meta(charset='UTF-8')
  meta(http-equiv='X-UA-Compatible' content='IE=edge')
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  style 
    include kasse.css
    include common.css
  script(src="server.js")
  title PaperSign
.tabbtn
  form(action = "/kasse" method="get")
    input(class="button" type="submit" value="Kasse")
  form(action = "/faktura" method="get")
    input(class="button" type="submit" value="Faktura")
  form(action= "/crud" method="get")
    input(class="button" type="submit" value="Crud")
h1 PaperSign Kasse
.parent
  .child1
    form.div2(type="hidden" action = "/search", method="get")
      input#atribut(type='text' name="attribut" placeholder='Attribute')
      input#value(type='text' name="vaerdi" placeholder='Søgeværdi')
      input(class='button' type="submit", value="Søg")
    form#pg(type="hidden" action = "/kasse", method="get")
      select#pg(name='pgroup' onchange="this.form.submit()")
        option(value='' disabled='' selected='') Produktgrupper
        option(value='') *Vis Alle
        each d of produktgrupper
          if(d.gruppeNr==qnr)
            option(value=""+d.gruppeNr+"" selected)= '' + d.navn
          else 
            option(value=""+d.gruppeNr+"")= '' + d.navn
        option(value='') *Intet
    p#titleP Produkter
    select#produktList(name='kurv' multiple='')
      each data in produkter
        option(value="" placeholder="")= '' + data.navn
    form.div3(type="hidden" action = "/kasse", method="post") 
      input#antal(type='number' name="antal" placeholder='Antal')
      button#tilfoejProdukt(class='button' type="submit") Tilf&oslash;j
  .child2
    p#titleB  
    p#titleB
    p#titleK Kurv
    select#kurvList(name='kurv' multiple='')
      if (kurv!=null)
        each k of kurv
          option(value=""+k.nanv+"" selected)= '' + k.navn
    button#tilfoejRabat Tilf&oslash;j Rabat
    input#rabat(type='number' placeholder='Rabat')
    button Slet
    select#betalling(name='kurv' onchange= "if (this.selectedIndex==2) alert(document.getElementById('pg').value);")
      option(value='' disabled='' selected='') Betallingsform
      option(value='kort') Kort
      option(value='kredit') Kredit
      option(value='kontant') Kontant
      option(value='mobilPay') MobilPay
    input#Beloeb(type='number' name='' placeholder='Beløb')
    button#koeb K&oslash;b
