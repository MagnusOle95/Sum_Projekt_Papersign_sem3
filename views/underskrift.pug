
script.
    async function post(url, objekt) {
    const respons = await fetch(url, {
    method: "POST",
    body: JSON.stringify(objekt),
    headers: {
    'Content-Type': 'application/json'
    }
    });
    if (respons.status !== 201) // Created
    throw new Error(respons.status);
    return await respons.text();
    }

    let opretProdukt = async () => {
        console.log("Når her til")
        var pNavn = document.getElementById("pNavn").value;
        try {
        await post("/opretProdukt", {pNavn: pNavn});
        window.location.href = "/"
        }catch(e){
        console.log("Fejl");
        } 
    }

    let accepterUnderskrift = async (beskedId) => {
        try {
            // mangler funktioner (og korrekt redirection?)
        window.location.href = "/underskrift"
        }catch(e){
        console.log("Fejl");
        } 
    }


body 

    h1 Underskrift 
        <br>
        button(onclick='accepterUnderskrift()') Acceptér Underskrift 

    
         
        
        
        
